# 2026-03-30

## Signal Loom Sprint 2.5a — Complete

### Brief received
`sprint_2_5a_hotfix_brief.md` — 5 regressions from Sprint 2.5.

### Rules doc written
`SPRINT2_5a_NERO_RULES.md` — pane fallback rules + sidebar routing logic.
Two corrections applied per Likwid review:
1. `activePaneId` as sole active truth (no inline `pane.active` recompute on close)
2. Monitor pane step 3 removed from sidebar routing (monitor is NEVER implicit sidebar target)

### Fixes applied

**Fix 1 — Shell layout top bar overlap (§4.1)**
- Shell height chain: `h-screen` root → `TopBar` → `flex-1 min-h-0` content row → `NeroWorkspace` fills remaining → `flex-1 min-h-0` center → panes `h-full`
- RuntimeStrip docked at bottom of flex column

**Fix 2 — Delegation timeline hydration mismatch (§4.2)**
- `TimelineEvent` extracted as sub-component
- `useState(event.createdAt)` + `useEffect(() => { setTime(formatRelative(...)) }, 0 })` pattern
- Server renders ISO string; client updates to relative after mount
- ✅ Verified: zero console errors on fresh load

**Fix 3 — Pane close behavior (§4.3)**
- `derivePresetFromPanes()` helper added to store
- `closePane(paneId)` action added to store implementing:
  - Primary pane close: BLOCKED
  - Last non-monitor pane close: BLOCKED
  - `activePaneId` sole active truth
  - Fallback priority: primary → secondary → monitor → first remaining
- `thread-pane.tsx`: close button condition changed from `isActive && onClose` to `onClose` — secondary pane close button now visible regardless of active state ✅
- `monitor-thread-pane.tsx`: `onClose?` optional, removed framer-motion `whileHover` layout animation

**Fix 4 — Sidebar thread selection routing (§4.4)**
- No code changes needed — `selectThread` in store already implements correct routing
- Verified: sidebar click routes thread to active pane correctly

**Fix 5 — Motion cleanup (§4.5)**
- `nero-workspace.tsx`: removed `layout` prop from pane `motion.div` — biggest resize jank source
- Pane transitions: pure opacity only (`opacity: 0 → 1`)
- Monitor collapsed strip: CSS `transition: width 0.2s ease` + `:hover` style replaces framer `whileHover` layout animation
- All transitions: `duration: 0.15–0.18s, ease: easeOut`

### Bug found during verification
**Close button only on active pane**: `thread-pane.tsx` had `isActive && onClose` — close button only showed on the active pane. Fixed to `onClose` — close button now shows on all closeable panes regardless of active state.

### Bug found during verification — hydration error
**`useState` initializer with setTimeout**: Original `useState(() => { setTimeout(...) })` — initializer runs during render and schedules async state update, causing "Can't perform React state update on unmounted component" error. Fixed to proper `useEffect` pattern.

### Build status
✅ `npm run build` — clean, zero TypeScript errors

### Visual verification (4 gates)
- **Gate 1 — Layout / top bar**: ✅ PASS — Focus mode, Duo mode, all presets show no overlap
- **Gate 2 — Pane close + sidebar routing**: ✅ PASS — Secondary pane close works, returns to Focus cleanly, sidebar routing works after close
- **Gate 3 — Hydration**: ✅ PASS — Zero console errors on fresh load
- **Gate 4 — Motion**: ✅ PASS — Preset switch clean, no layout flicker, no jank

### Status: ✅ ACCEPTED — Sprint 2.5a complete

---
