# Memory — 2026-03-27

## Claw3D Installation Attempt (Windows, not WSL)

- Correct Claw3D repo: `https://github.com/iamlukethedev/Claw3D` — **Luke's dev** account, NOT Likwid
- WSL2 memory ceiling blocked all Next.js builds (OOM killed at ~1.7GB available)
- Decided to install Claw3D natively on Windows instead of WSL
- Claw3D installed at: `C:\Users\likwi\claw3d`
- Claw3D .env.local configured:
  ```
  HOST=127.0.0.1
  PORT=3000
  NEXT_PUBLIC_GATEWAY_URL=ws://localhost:18789
  ```
- Port 3000 was in use — killed process PID 32388 to free it
- Claw3D dev server started with `npm run dev`

## Windows Browser → WSL Gateway Access Issue (UNRESOLVED)

- WSL gateway is accessible from WSL Chrome and via PowerShell curl (HTTP 200 confirmed)
- `localhost:18789/chat` returns 200 via `curl localhost:18789`
- But Likwid cannot access the webchat from his Windows browser at `http://127.0.0.1:18789/chat`
- This is a browser/network-layer issue on Windows, NOT a gateway issue
- Possible causes: browser extension, Windows Defender/firewall, proxy settings, Chromium network stack
- Status: UNRESOLVED — needs further troubleshooting from Windows side

## Session Continuity

- WSL was restarted during troubleshooting — gateway came back up fine
- Gateway token: `5a420c42-3c6c-4357-8d5d-f1e5b9a3c7a5` (unchanged)
- Gateway running normally on `ws://localhost:18789`

## Open Issues

- Windows browser cannot reach `http://127.0.0.1:18789/chat` despite gateway being reachable via curl
- Needs: browser DevTools Network tab check, browser/extension audit, Windows Defender review

## Update 00:17 — RESOLVED

- Windows browser can now reach `http://127.0.0.1:18789/chat` again
- Issue resolved without specific fix identified — likely a transient WSL networking state that cleared after restart

## SimSuite UI Work (late night)

- Major SimSuite Downloads UI pass completed and pushed to `main`.
- Key fixes shipped:
  - accessible animated tooltip component for Settings (replaced native `title` tooltips)
  - Casual drawer close bug fixed (lane-selection effect was re-selecting the first item after close)
  - Casual viewport fill fix so Downloads stretches better when maximized
  - close-button cleanup (`×` → proper icon/button styling), darker drawer backdrop, better keyboard-hint close affordance
  - Casual Settings summaries compacted into chips instead of verbose cards
  - Seasoned redesign: removed always-visible inspector so the preview panel is the single decision surface
  - Creator redesign: inspector is now collapsible and starts collapsed; preview footer carries approve/reject actions so work can continue without opening the inspector
  - Seasoned/Creator Settings layout rebalanced and right-side summary panel improved
- Important design decision: Casual = overlay drawer; Seasoned = preview panel as decision surface; Creator = preview panel + optional collapsible inspector.
- Remaining work: do a slower production-readiness UI polish pass using desktop smoke / Playwright against the Windows-hosted Tauri app to catch subtle visual bugs across sizes and modes.

## OpenClaw Config

- Updated `~/.openclaw/openclaw.json` so `agents.defaults.thinkingDefault = "xhigh"`.
- Also set every configured agent's `thinkingDefault` to `"xhigh"` for consistency.
- Gateway status checked after the config change: running normally on `127.0.0.1:18789`.
