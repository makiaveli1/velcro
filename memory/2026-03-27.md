# Memory — 2026-03-27

## Claw3D Installation Attempt (Windows, not WSL)

- Correct Claw3D repo: `https://github.com/iamlukethedev/Claw3D` — **Luke's dev** account, NOT Likwid
- WSL2 memory ceiling blocked all Next.js builds (OOM killed at ~1.7GB available)
- Decided to install Claw3D natively on Windows instead of WSL
- Claw3D installed at: `C:\Users\likwi\claw3d`
- Claw3D .env.local configured:
  ```
  HOST=127.0.0.1
  PORT=3000
  NEXT_PUBLIC_GATEWAY_URL=ws://localhost:18789
  ```
- Port 3000 was in use — killed process PID 32388 to free it
- Claw3D dev server started with `npm run dev`

## Windows Browser → WSL Gateway Access Issue (UNRESOLVED)

- WSL gateway is accessible from WSL Chrome and via PowerShell curl (HTTP 200 confirmed)
- `localhost:18789/chat` returns 200 via `curl localhost:18789`
- But Likwid cannot access the webchat from his Windows browser at `http://127.0.0.1:18789/chat`
- This is a browser/network-layer issue on Windows, NOT a gateway issue
- Possible causes: browser extension, Windows Defender/firewall, proxy settings, Chromium network stack
- Status: UNRESOLVED — needs further troubleshooting from Windows side

## Session Continuity

- WSL was restarted during troubleshooting — gateway came back up fine
- Gateway token: `5a420c42-3c6c-4357-8d5d-f1e5b9a3c7a5` (unchanged)
- Gateway running normally on `ws://localhost:18789`

## Open Issues

- Windows browser cannot reach `http://127.0.0.1:18789/chat` despite gateway being reachable via curl
- Needs: browser DevTools Network tab check, browser/extension audit, Windows Defender review

## Update 00:17 — RESOLVED

- Windows browser can now reach `http://127.0.0.1:18789/chat` again
- Issue resolved without specific fix identified — likely a transient WSL networking state that cleared after restart

## SimSuite UI Work (late night)

- Major SimSuite Downloads UI pass completed and pushed to `main`.
- Key fixes shipped:
  - accessible animated tooltip component for Settings (replaced native `title` tooltips)
  - Casual drawer close bug fixed (lane-selection effect was re-selecting the first item after close)
  - Casual viewport fill fix so Downloads stretches better when maximized
  - close-button cleanup (`×` → proper icon/button styling), darker drawer backdrop, better keyboard-hint close affordance
  - Casual Settings summaries compacted into chips instead of verbose cards
  - Seasoned redesign: removed always-visible inspector so the preview panel is the single decision surface
  - Creator redesign: inspector is now collapsible and starts collapsed; preview footer carries approve/reject actions so work can continue without opening the inspector
  - Seasoned/Creator Settings layout rebalanced and right-side summary panel improved
- Important design decision: Casual = overlay drawer; Seasoned = preview panel as decision surface; Creator = preview panel + optional collapsible inspector.
- Remaining work: do a slower production-readiness UI polish pass using desktop smoke / Playwright against the Windows-hosted Tauri app to catch subtle visual bugs across sizes and modes.

## OpenClaw Config

- Updated `~/.openclaw/openclaw.json` so `agents.defaults.thinkingDefault = "xhigh"`.
- Also set every configured agent's `thinkingDefault` to `"xhigh"` for consistency.
- Gateway status checked after the config change: running normally on `127.0.0.1:18789`.

## SimSuite ConflictEvidenceDisplay — Implementation & Rollout Validation

### What was built
`ConflictEvidenceDisplay` — a read-only Inbox Creator diagnostic component that surfaces backend conflict evidence in the Decision panel for power users during inbox triage.

**Files:**
- `src/screens/downloads/ConflictEvidenceDisplay.tsx` — new, pure display, no state
- `src/screens/downloads/downloadsDisplay.ts` — added `ENABLE_CONFLICT_EVIDENCE = false` flag
- `src/screens/downloads/DownloadsDecisionPanel.tsx` — +versionResolution/+specialDecision props + conditional render
- `src/DownloadsScreen.tsx` — prop passing + flag import
- `src/styles/globals.css` — scoped `.conflict-evidence-*` CSS classes

### ADR
`CONFLICT_EVIDENCE_DISPLAY_ADR.md` — documents Inbox-only boundary, read-only contract, confidence language rules, enforcement.

### Label fixes applied (Ariadne feedback)
- "Review suggested" → "Needs review" (more actionable)
- "Unclear — review recommended" → "Unclear — verify first" (tells user what to do)
- Seasoned row badge: "Conflict found" → "Needs review"

### Validation results
- Flag-off: baseline identical ✅
- Flag-on Creator: 5-scenario walkthrough complete ✅
- Cross-app non-interference: confirmed (Library, Updates, Needs Review, Tidy Up all untouched) ✅
- UX review: Ariadne ✅ Helpful; labels improved ✅
- Boundary review: Sentinel ✅ Holding; no setters, no cross-screen imports
- TypeScript: 0 errors ✅

### Rollout recommendation
Ship behind flag. Enable for Creator in limited rollout. Keep disabled in mainline. Rollback = flip to false.

### Hardening gap (acknowledged, not implemented)
No ESLint import boundary to prevent future import of ConflictEvidenceDisplay outside screens/downloads/. Manual code review is the enforcement. Future hardening item.

### SimSuite review workspace
`/home/likwid/.openclaw/workspace/simsuite-review/` — mirrors the actual project at `/mnt/c/Users/likwi/OneDrive/Desktop/PROJS/SimSort-main-merge-20260319`

---

## CRM 2.0 Build — Phases 1-3 Complete (Today)

### What was built
Full relationship intelligence CRM at `ventures/website-studio/CRM/` — replaces the old file-based lead tracker.

### Phase 1 — Foundation
- `db/schema.sql`: 15 tables (contacts, companies, interactions, contact_context, contact_summaries, follow_ups, meetings, meeting_attendees, meeting_action_items, company_news, discovery_review, skip_patterns, email_drafts, daily_digest_log, schema_version) + indexes
- `db/database.js`: better-sqlite3 WAL mode, auto-init
- `server.js`: Express API on port 3100 — full CRUD for all entities, discovery queue, dashboard, NL query, health
- `adapters/`: graph.js (stub), llm.js (stub), embeddings.js (stub), messaging.js (stub)
- `migrations/001_lead_migration.js`: 12 leads migrated from LEADS/ → SQLite
- `UI_DESIGN_SYSTEM.md`: full design system — amber #E8A445 on #0A0A0C, component tokens, motion, NL bar spec
- `UI_WIREFRAMES.md`: screen-by-screen wireframes for all 10 screens
- `package.json`: verdantia-crm v2.0.0

### Phase 2 — Discovery + Intelligence
- `adapters/graph.js`: Full Graph adapter — OAuth2 device code flow, token refresh, getRecentMessages/Events, extractContactsFromMessages/Events, noise filtering (internal domain, noreply, large meetings)
- `services/discovery.js`: runDiscovery() pipeline, matchesSkipPattern(), seedMockDiscovery(), auto-add threshold (50 decisions)
- `services/scoring.js`: recency×frequency×priority×signal model (0-100), scoreContact/scoreAllContacts, getScoreBreakdown, generateNudge, getContactsNeedingAttention
- `services/daily.js`: runDailyDigest() — scan + score + attention + digest build + notification; quickDigest() DB-only
- `adapters/messaging.js`: OpenClaw gateway integration for webchat delivery
- `cron/daily.js`: standalone cron entry with --dry-run/--quiet-hours/--no-notify flags
- OpenClaw cron: daily digest at 8:30 AM Mon-Fri Europe/Dublin (job created, confirmed in cron list)

### Phase 3 — LLM Integration
- `adapters/llm.js`: MiniMax Anthropic API adapter, generateContactSummary (relationship_type/comms_style/key_topics), generateEmailDraft (tone control, context-grounded), parseQuery (LLM intent + rule-based fallback), regenerateStaleSummaries
- `services/summaries.js`: generateSummary(), getSummary(), enrichFromSummary()
- `services/drafts.js`: generateDraft (always 'proposed' status), approveDraft, rejectDraft, getDraftsForContact, validateTone
- Two-phase approval: all drafts stored proposed → explicit approve → user opens email client to send (CRM has no send access, intentional)
- NL query upgraded: 8 intent types (about_contact, who_needs_attention, stats, weekly_summary, followup_list, discovery_queue, search_contacts)

### Phase 6 — UI (Ariadne built)
- React 18 + Vite SPA at `CRM/ui/`
- Dev server: http://localhost:5173 (Vite proxy → localhost:3100)
- 24 source files — full component library, all 7 pages
- Design: Dark Intelligence Terminal aesthetic, amber #E8A445, Inter + JetBrains Mono

### API Server
- Running on port 3100
- Key endpoints: GET/POST /api/contacts, /api/discovery, /api/followups, /api/scoring/breakdown/:id, /api/attention, /api/summaries/:id, /api/drafts, /api/query, /api/daily/run, /api/config
- Key bug fixed during Phase 3: duplicate route handlers (Forge's old routes + new ones) — removed duplicate handlers from server.js
- Key bug fixed: pollForToken used process.env instead of config object — patched

### CRM Config
- Domain: verdantia.it (corrected from verdantia.ie)
- MY_EMAIL_DOMAIN=verdantia.it for internal email filtering

---

## Website Studio — Mailbox Readiness

### studio@verdantia.it Shared Mailbox
All docs updated to reflect shared mailbox path, not individual user mailbox.

### Files updated
- README.md: outbound blocked until mailbox checklist complete
- OUTREACH_POLICY.md: hard block + Graph + studio@verdantia.it
- CHANNEL_POLICY.md: Graph shared mailbox, Rule 6
- MAILBOX_SETUP.md (new): 10-item checklist, Nero sign-off block
- RUNBOOKS/APPROVAL_QUEUE.md: Mailbox Readiness as Section 1 hard gate

### Mailbox Readiness Checklist (MAILBOX_SETUP.md)
1. Mailbox exists — ✅ pre-confirmed
2. Full Access permission (Exchange) — pending
3. Send As permission (Exchange) — pending
4. Manual Send As test (OWA) — pending
5. Azure app registered — ✅ done
6. Graph credentials stored — ✅ done
7. Sent Items approach confirmed — pending
8. DNS/domain verified — pending
9. Outreach template verified — pending
10. Nero sign-off — BLOCKED

### Key decision: Sent Items
Graph /me/sendMail always saves to authenticated user's Sent Items, not shared mailbox.
v1 decision: User Sent Items + CRM as authoritative log. No shared mailbox Sent Items sync in v1.

---

## Azure App Registration

- App name: Verdantia CRM
- Client ID: 89f55189-c396-4444-b4fe-b03301c26d68
- Tenant ID: 8669ffe2-3fed-463c-a64b-eff1dd4a34c8
- Object ID: 6d56d72e-bc69-4eea-bf25-13975188124b

### Config file
`ventures/website-studio/CRM/config/graph.json` — pre-filled with real credentials

### Required permissions (Delegated)
- Mail.Send.Shared — send AS studio@verdantia.it
- Mail.Read — email discovery
- Calendars.Read — calendar/meeting discovery
- offline_access — refresh tokens

### Auth model
Device Code Flow (public client, no client secret, no redirect URI for the flow itself)
Platform: Mobile and desktop applications, redirect URI: http://localhost
Treat as public client: Yes

### Critical bugs found and fixed
1. pollForToken used process.env.GRAPH_CLIENT_ID instead of config.clientId — silent auth failures on refresh
2. Azure device code response uses snake_case (device_code, user_code) — adapter normalized to camelCase

### Auth status
Interactive auth ran (session tidal-daisy, PID 8464) — user completing login in browser.
Code: L5M4BCG6T. Token NOT yet confirmed saved (process was still polling when session ended).

---

## Current State — Evening

- CRM API: running on port 3100 ✅
- CRM UI: running on port 5173 (dev) ✅  
- Graph adapter: needs re-auth (token not yet confirmed saved)
- Website Studio outreach: fully BLOCKED pending mailbox checklist
- Daily digest cron: scheduled 8:30 AM Mon-Fri ✅

### Outstanding Graph auth
Need to re-run: `cd ventures/website-studio/CRM && node -e "require('./adapters/graph').setupInteractive()"`
Then verify token at config/graph_token.json.
Then Exchange Admin: Full Access + Send As for the licensed user on studio@verdantia.it
Then manual OWA Send As test.
Then Nero sign-off in MAILBOX_SETUP.md.

### Outstanding before outreach can begin
1. Complete Graph device code login (pending)
2. Verify token saved
3. Exchange Full Access + Send As (pending — user says done)
4. Manual OWA Send As test
5. DNS/domain verification
6. Nero sign-off
