# Memory — 2026-03-28

## Session Overview
Long, fractured session. Gateway crashed multiple times due to zombie background node processes. Many attempted auth flows failed. Final state is partially resolved but Send As API test is STILL blocked on user completing device code login in a terminal.

---

## What Was Completed

### Graph Auth ✅ (finally)
- Token acquired at ~00:12 GMT, expired ~01:32
- Scope includes `Mail.Send.Shared` — correct for Send As
- Token file: `ventures/website-studio/CRM/config/graph_token.json`
- `/me` identity: `oluwagbemi@verdantia.it` — confirmed
- Inbox and calendar accessible
- Refresh token present

### OWA Send As ✅
- User manually confirmed Send As from OWA works (sent test email)

### Daily Digest Cron ✅
- Scheduled at 8:30 AM Mon–Fri via OpenClaw gateway
- Job ID: `18bacc2a-e6be-4e56-b87b-6db8e56a8b78`
- Session target: `isolated`, delivery: `announce`

### Ariadne UI — Built ✅
- React 18 + Vite SPA, Dark Intelligence Terminal design
- 24 source files in `ventures/website-studio/CRM/ui/`
- Dev server: `http://localhost:5173` (Vite proxy → `:3100`)
- 7 pages: Discovery, Contacts, Contact Detail, Follow-ups, Drafts, Settings, Approve
- LLM adapter built at `adapters/llm.js` (MiniMax Anthropic API)

### Security Incidents Resolved
- `graph_token.json` removed from git index AND filesystem (was committed briefly)
- `openclaw-github-assistant` skill deleted — credential harvesting patterns

---

## What Is BROKEN / BLOCKED

### Send As API Test 🔴 STILL BLOCKED
This is the last technical gate before outreach clearance.
- Token from 00:12 session has likely expired
- Multiple device code flows attempted, all failed due to:
  - Background exec hanging/killed before polling completed
  - Python script polling bug (HTTP 400 with empty body on repeated polls — rate limited by Microsoft)
  - Gateway crashes from zombie node processes
- **Current code (as of 01:36): `GSPL7H9ST`** — may be expired or fresh depending on whether a new code was generated

### DNS / Domain Verification 🔴
- Checklist item 6 in `MAILBOX_SETUP.md` — not started

### Nero Sign-off on MAILBOX_SETUP.md 🔴
- Pending all checklist items

### LLM Integration 🔴
- MiniMax API key not yet in `config/llm.json`
- `services/daily.js` not yet connected to LLM for personalized digest

---

## Key Scripts Created

### `CRM/scripts/auth_sendas.py` — USE THIS
- Self-contained Python (stdlib only)
- Gets device code → polls → saves token → runs Send As test automatically
- Fixed polling bug (HTTP 400 rate-limit handling)
- To run: `cd /home/likwid/.openclaw/workspace/ventures/website-studio/CRM && python3 scripts/auth_sendas.py`
- Run in a LOCAL terminal (WSL/Linux) — NOT through the web UI exec (which kills background processes)

### `CRM/scripts/do_all.js`
- Node.js version of the same flow
- Has path issue (looks for `scripts/config/graph.json` instead of `config/graph.json`)
- Use `auth_sendas.py` instead

---

## Exec Subsystem Status
- BROKEN since ~01:20 GMT
- Background node processes from earlier auth attempts are locking exec
- nohup background processes also dying (Python script exits with "Timed out" after one poll)
- Gateway needs full recovery/restart to restore exec
- Subagent spawning also failing for exec-backed operations

---

## Active Subagents (as of 01:42)
- `graph-sendas-test` (agent:main:subagent:8dddf78e...) — running, 3m runtime
- `auth-sendas-final` (agent:main:subagent:374e8e8d...) — running, 1m runtime
- `forge-crm-phase1` (agent:main:subagent:591dbd24...) — running, 14h50m
- `scout-verification-pass` (agent:main:subagent:ee04257f...) — running, 2d3h

---

## Next Steps (Priority Order)

1. **User runs `auth_sendas.py` in a local terminal** — this is the only remaining unblocked step before outreach clearance
2. **Send As API test passes** → update `MAILBOX_SETUP.md` checklist item 5
3. **DNS verification** for `verdantia.it`
4. **Fill MiniMax API key** in `config/llm.json`
5. **Wire `services/daily.js` to LLM** for personalized digest
6. **Nero sign-off** on `MAILBOX_SETUP.md`
7. **Full end-to-end UI test** across all 7 pages

---

## File Locations (Canonical)
- CRM root: `/home/likwid/.openclaw/workspace/ventures/website-studio/CRM/`
- UI root: `/home/likwid/.openclaw/workspace/ventures/website-studio/CRM/ui/`
- Token: `CRM/config/graph_token.json`
- Config: `CRM/config/graph.json`
- Auth script: `CRM/scripts/auth_sendas.py`
- Daily digest cron: `18bacc2a-e6be-4e56-b87b-6db8e56a8b78`
