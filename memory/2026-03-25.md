# 2026-03-25 — SimSuite Inbox Fixes + UI/UX Review Started

## Session Overview
- Morning heartbeat: Telegram polling stall detected, auto-restarted
- Full session focused on SimSuite fixes + UI/UX audit start

---

## What Happened

### F3 (MCCC Auto-Update) Cleanup ✅
- The backend commands `check_mccc_update` and `apply_mccc_update` were removed in the 2026-03-24 session
- Frontend still had broken references causing JS errors
- **Removed:** `McccUpdateInfo` and `ApplyMcccUpdateResult` types from types.ts
- **Removed:** `checkMcccUpdate()` and `applyMcccUpdate()` from api.ts  
- **Removed:** MCCC state (`mcccUpdateInfo`, `loadingMccc`, `applyingMccc`), `loadMcccUpdateInfo()`, `handleApplyMcccUpdate()`, and the entire MCCC card JSX from UpdatesScreen.tsx
- **Result:** No more JS errors from missing backend commands

### Downloads Counter Bug Investigation 🔍
- User reported: all lane counters showing 0 even though backend has data
- Database confirmed: 8 non-rejected/ignored items with correct status distribution
- **SQL is correct** — `load_overview()` returns accurate counts (total=8, ready=6, needs_review=2, etc.)
- **Root cause found:** After `load_overview()` computed correct counts, lines 503-524 then OVERWROTE them with per-item lane recalculation based on `items.len()`. If the items query ever returned 0 rows, all counters would become 0.
- **Also fixed:** `rejected_items` was always 0 because it was computed inside the WHERE-filtered result (rejected rows excluded). Now uses uncorrelated subquery.
- **Fix committed:** `2f65d9c` in SimSuite repo

### UI/UX Review 🔍 (in progress)
- Reviewed full codebase — most prior audit items are ALREADY implemented
- Remaining genuine issues:
  1. Downloads counter (being fixed)
  2. Confirmation consistency across destructive actions
  3. Settings hint tooltips (native `title` attribute → popover)
  4. No "Rejected" items browser UI (data exists but no recovery UI)
  5. Updates search is client-side (may not scale)

---

## Memory / State Files

### heartbeart-state.json
- Updated lastChecks.git to now

---

## Active Work

### UI/UX Deep Dive (in progress)
- User wants full review of all pages, layouts, user experiences
- Needs: layout audit, UX consistency check, user flow validation
- Status: Started — need to systematically go through all screens

### Downloads Counter (committed, awaiting user test)
- Backend SQL fixed and committed
- User needs to rebuild and test the app to verify counters now show

---

## Technical Notes

### Key SimSuite Paths
- Project: `/mnt/c/Users/likwi/OneDrive/Desktop/PROJS/SimSort`
- DB: `C:\Users\likwi\AppData\Roaming\com.likwi.simsuite\simsuit\simsuit.sqlite3` (18MB, 19 items)
- Config: `app_settings` table has downloads_path = `C:\Users\likwi\Downloads`

### Key Rust Files
- `src-tauri/src/core/downloads_watcher/mod.rs` — main downloads logic
- `src-tauri/src/commands/mod.rs` — Tauri commands

### Key Frontend Files  
- `src/screens/DownloadsScreen.tsx` — downloads inbox
- `src/screens/UpdatesScreen.tsx` — updates/mods management
- `src/lib/api.ts` — Tauri API bindings
- `src/lib/types.ts` — TypeScript interfaces

---

## Commits This Session (SimSuite)
- `2f65d9c` — fix(simsuite): remove broken F3 MCCC refs + repair Downloads counter SQL bugs
- `3d93f69` — fix(simsuite): add confirmation dialogs to batch apply and batch reject actions

## Confirmation Consistency Audit
- **DownloadsScreen:** Confirmed — single reject ✅ has dialog, batch reject ✅ now has dialog, batch apply ✅ now has dialog
- **OrganizeScreen:** No destructive actions found
- **ReviewScreen:** No destructive actions found  
- **UpdatesScreen:** No destructive actions (just informational text)
- **LibraryScreen:** No destructive actions found

## UI/UX Audit Status (subagent running)
- Full audit underway — checking all 20+ findings from UI_AUDIT.md
- Text-dim contrast: analyzed — all current uses are decorative labels/badges that pass WCAG AA; not a violation
- Mode transition guidance: would need toast infrastructure; noted for design pass
- Settings descriptions: subagent checking
- Guided flows (Casual): subagent checking
